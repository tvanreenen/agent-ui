{
  "scripts": {
    "build": "esbuild src/agent-ui-2/index.ts --bundle --outfile=dist/agent-ui.umd.js --format=iife --target=es2020 --sourcemap --minify && cp dist/agent-ui.umd.js examples/ && cp dist/agent-ui.umd.js.map examples/",
    "server": "npx serve examples -p 3000"
  },
  "dependencies": {
    "dompurify": "^3.0.8",
    "lit": "^3.3.1",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  }
}
