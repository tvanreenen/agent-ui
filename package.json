{
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": "^22.13.0 || >=24.0.0"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/agent-ui.js --format=iife --target=es2020 --sourcemap --minify && cp dist/agent-ui.js* examples/",
    "server": "pnpm exec serve examples -p 3000",
    "test": "vitest run --environment jsdom"
  },
  "dependencies": {
    "@ag-ui/client": "^0.0.57",
    "dompurify": "^3.4.12",
    "lit": "^3.3.3",
    "marked": "^18.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^30.0.0",
    "serve": "^14.2.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
