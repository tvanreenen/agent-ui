{
  "scripts": {
    "build": "esbuild src/agent-ui.ts --bundle --outfile=dist/agent-ui.umd.js --format=iife --target=es2020 --sourcemap --minify",
    "start": "npx serve examples -p 3000"
  },
  "dependencies": {
    "lit": "^3.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  }
}
