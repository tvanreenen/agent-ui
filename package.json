{
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/agent-ui.js --format=iife --target=es2020 --sourcemap --minify && cp dist/agent-ui.js* examples/",
    "server": "npx serve examples -p 3000"
  },
  "dependencies": {
    "@ag-ui/client": "^0.0.35",
    "dompurify": "^3.0.8",
    "lit": "^3.3.1",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.8.3"
  }
}
